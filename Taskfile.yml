# https://taskfile.dev

version: '3'

includes:
  api:
    taskfile: ./api/Taskfile.yml
    dir: ./api
  # frontend:
  #   taskfile: ./frontend/Taskfile.yml
  #   dir: ./frontend

tasks:
  default:
    cmds:
      - task -l
    silent: true

  build-all:
    desc: Builds docker images for all components 
    cmds:
      - task: api:build
      