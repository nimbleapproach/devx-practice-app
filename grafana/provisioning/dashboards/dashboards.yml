apiVersion: 1

providers:
  - name: 'prometheus'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    # could be 'true' for dev only
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: true